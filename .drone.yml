kind: pipeline
type: kubernetes
name: default

clone:
  disable: true

steps:
  - name: clone
    image: busybox
    commands:
      - curl https://github.com
      # - git clone https://github.com/LouisKamp/rust-test
      # - git checkout $DRONE_COMMIT
