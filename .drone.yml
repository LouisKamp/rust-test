kind: pipeline
type: kubernetes
name: default

# clone:
#   disable: true

steps:
  - name: test
    image: rust:1.30
    commands:
      - ls
      # - cargo build --verbose --all
      # - cargo test --verbose --all
