kind: pipeline
type: kubernetes
name: default

# clone:
#   disable: true

steps:
  - name: test
    image: rust:latest-slim
    commands:
      - cargo build --verbose --all
      - cargo test --verbose --all
